<li id="review-<%= review.id %>">
	<span class="user"><%= link_to review.user.name, review.user %></span>
	<span class="book"><%= link_to review.book.title review.book.author, review.book %></span>
	<span class="timestamp">
		Posted <%= time_ago_in_words(review.create_at) %> ago.
	</span>
</li>