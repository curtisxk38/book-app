<div class="center jumbotron">
  <h1>Welcome to the Book App</h1>

  <h2>
    This is the home page for a Book App.
  </h2>
  <% if user_signed_in? %>
  	<%= render 'layouts/home_logged_in' %>
  <% else %>
  	<%= render 'layouts/home_not_logged_in' %>
  <% end %>
</div>
